#include<stdio.h>
printf("sorted element:);
for(i=0;i<n;i++)
{
 printf("%d",a[1])
}
first=0;
last=n-1;
printf("enter two element");
scanf("%d",2key);
while(first<=last)
{
  mid=(first+last)/2;
  if(key==a[mid])
  {
   
